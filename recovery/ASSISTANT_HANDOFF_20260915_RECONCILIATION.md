# Assistant handoff reconciliation (2026-09-15)

Reference archive: `JIUYIN_STAGE37_NEW_CHAT_HANDOFF_20260915_BIND_LUA_CURRENT.zip`. Original ZIP SHA256: `e898008a0ec1aef39d16663835c9f479daed6e643c4c90252c5adafc3f4ffe1e`. Its `STAGE37_CURRENT_WORKTREE` is the editable assistant-produced Go snapshot. The original `9yin-go-server1.rar` is the server's source lineage, **not** a file to overwrite current source with; never substitute `9yin-go-server.zip`, JYZJ, V37 or V46 for the authoritative editable worktree.

This branch starts at Stage41 commit `1ce3b05ea419f0658ad88473b735b36654512d79` to **exclude 47 later commits** present on `stage37-current-recovery-20260916` at `f9991b6356c60e425f80bb20f5a8a99e33d49926`. The old branch/commits are deliberately retained for recovery. This branch is NOT yet a byte-for-byte import of the assistant ZIP: `server/cmd/protocol-probe/latest_client_shop_condition_authority.go`, `_evaluator.go`, and `_authority_integration_test.go` were observed to match ZIP Git blob SHA values, whereas `server/cmd/protocol-probe/main.go` and `latest_client_shop_exchange_contract.go` differ. Do not claim exact source synchronization until **every** file in the handoff manifest has been compared, reconciled and hashed. Only merge evidence-backed changes; no force push or destructive history rewrite.

Latest handoff's verified conceptual state (analysis is not production implementation): current-client Lua protection decoded; `lua64/custom_handler.lua` `on_open_shop_exchange_form` is the next instruction-level analysis target, zlib stream offset `0x147A62D`; S2C557 -> 11-field exchange config -> `InitCurExchangeData` -> form UI chain requires proof. Fields: `Type|AddValue|BindStatus|Item|ShowBind|ExchangeBind|ConditionType|Condition|Condition2|Filters|Prop`. Native offsets: BindStatus +0xD8, ExchangeBind +0xDC, ShowBind +0xE0. `bindindex` is a GoodsGrid/View binding index, NOT runtime BindStatus. C2S 0x40 args `(viewid,bindindex,shopid,page,pos)`, C2S 0x4F args `(shopid,page,pos,amount)`; both transaction paths stay OFF until exact ShowBind/result-bind calculations and DB atomicity/Bag/View reconciliation are proven.

ExchangeData: 8,575 unique, SAFE 2,422, FAIL_CLOSED 6,153. Source-level unit tests are NOT LIVE proof. Do not infer completion percentages from build status. Verify actual current resources by hashes in the archive's `CURRENT_AUTHORITY_HASHES.txt` before running runtime tests. No private client assets, original RAR, DB, personal role data, or Lua key are committed here.

**Next action:** compare archived `STAGE37_CURRENT_WORKTREE` against `server/` file by file, restore only verified assistant changes, run GitHub Actions using real Go dependencies and private resource fixtures where authorized, then perform Windows LIVE/E2E separately. A branch and this note do not mean reconciliation or deployment is complete.
