# Stage45 addendum — authenticated Lua64 getter call sites (2026-09-17)

## Identity and method

Continue Stage45 checkpoint `bfea8bf6128bfab0787861f3e22991deb4539db4`; this addendum does not supersede its provenance, server-state audit or fail-closed constraints. Latest Google Drive `bin64.zip` (`13KBBJ9nEQjPZngrI_Ald-u15zlV1MKnS`) SHA256 `0bfa5c66c874f6fdd78cb343d7c1ebff29490c2f5d3e7e10be45012bc0f3b2ec` contains `fxgame.exe`, 8,772,368 bytes, SHA256 `c7f0366026452d525dc0c74b8499aac30fdb69189591420978e12c7094b5fde3`. `fxgamelogic.dll` SHA256 `16cf49b652c39b63ba0767780cc39f32ea1e50bdc683da9d5e7b2697cf95c3e8` remains the binary getter-registration basis. Latest Drive `lua64.package` (`1NWh8NSeN-mQ2RdeJi9yCkX40uuDBW7j6`) SHA256 `700cb8c888374ed3ceb0f03b940e3f148620519f82352499f7957991184677d2`.

Read-only process: obtain the current `fxgame.exe` from the authenticated ZIP, locate its embedded Lua transformation key with the exact key-extraction method in original RAR `cmd/lua-codec/main.go:79-104`, decode each intact bounded zlib Lua64 payload using original RAR `internal/luacodec/codec.go:TransformWithKey`, then reverse-transform and byte-compare with the input. The key, raw EXE, DLL and decoded client Lua are **not** published. The transformed Lua64 chunks are checked as standard Lua 5.1 x64 little-endian bytecode, **parsed statically without execution**. A separately scanned 32-bit `lua.package` has a distinct 4-byte `size_t` layout and was NOT decoded by this x64-only codec; this is not evidence that it lacks the functions.

Scope of the bounded scan: `lua64.package` contained 2,292 recognized `\x1bLua` zlib payloads; 2,292/2,292 decoded successfully and 2,292/2,292 reversed byte-for-byte; the scan did not prove complete PCK0 index extraction. Earlier Stage45 *encoded-byte plaintext search* had zero getter matches; the authenticated decoding resolves that limitation, not a contradiction. Each source path below is the Lua function's **embedded source string**, not a filename recovered from the encrypted package index.

## Exact recovered Lua bytecode evidence

Source string `@G:\Version\01_Client\lua64\form_stage_main\form_shop\form_exchange.lua`. Compressed stream starts at original `lua64.package` file offset **18047301**. Encoded chunk: **26,500 bytes**, SHA256 `ace01ebc8104d4399f6a3ac08e0ebc6e8d40c855ac4512b3eb6efd4cdde6078f`. Decoded standard Lua 5.1 chunk: **26,500 bytes**, SHA256 `d40b705a8f4f2e5562879f6aa5a595dc4be3a9e5b64660279d8b311ac5b7ebe7`. Statically parsed full chunk: **29 function prototypes**; full buffer consumed without trailing bytes.

- Function declared at Lua source **lines 81–92**, bytecode PC **10–17**, calls `nx_execute("custom_sender", "custom_exchange_item", item_obj.shop_id, item_obj.page, item_obj.pos, amount)` at **line 88**. Bytecode PC 12 loads literal `custom_exchange_item`; PCs 13–16 load `shop_id`, `page`, `pos` and move the amount; PC 17 is CALL. This confirms a *client-side request call*, not its official server handler or settlement.
- Function declared at source **lines 485–551**, bytecode PC **133**, Lua **line 536**, invokes `exchange_item_manager:GetCurType()` (SELF PC 133 and CALL PC 134). PCs **135–142**, line **537**, convert the returned value with `nx_int`, compare it to the captured upvalue whose original debug name is `EXCHANGE_TYPE_GUILDBUILDING`, then conditionally enter a UI branch. The actual numeric value of this constant is **not** established by its name or this branch.
- In that same UI branch, bytecode PCs **145–146**, Lua **line 541**, invoke `exchange_item_manager:GetCurAddValue()`. PCs **147–153**, Lua **line 542**, convert that value and a second argument with `nx_number` and multiply them. PCs **154–158**, line **543**, call `get_rebate_value` on the resulting number. PCs **159–183**, lines **545–549**, format the result using text key `ui_guild_currency_9`, then send it to `groupbox_consume` / `mltbox_consume` HTML display. This is a **client UI cost-display arithmetic path**, not proof of the authoritative debit, reward, rebate, or Count rules.
- The matching `GetCurType` and `GetCurAddValue` names are exactly from this hash-verified decoded chunk, not merely DLL registration strings. The related function's debug upvalue name is `EXCHANGE_TYPE_GUILDBUILDING`; treating all `Type=3` or any `AddValue` as universal currency is specifically unsupported.

## Settlement and storage remain blocked

Neither the decoded Lua display branch nor the client `custom_exchange_item` sender establishes the official server's inventory/currency ownership, Item+Prop combination semantics, AddValue payment direction, item reward identifier, binding algorithm, capacity behavior, duplicate-request identity, or atomic SQL/JSON settlement. No 0x4F production code, DB migration, client binary or private package was modified. Actual 0x4F material debit/grant remains **FAIL-CLOSED**. No purchase, Windows build, server boot, SQL replay, database reconnect or live client test was run by this addendum.

**Next single priority:** inspect the same authenticated decoded `form_shop/form_exchange.lua` functions at lines **261–347** (`GetCurItemList`, bind methods) and **485–584** (`GetCurPropList`, `GetCurType`, `GetCurAddValue`) in bytecode/condition-flow detail; cross-map each displayed value to the actual Go actor and persistence owner without treating display arithmetic as official settlement. Keep fail-closed until a verifiable server-side settlement and atomic storage plan exists.
