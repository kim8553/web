# Stage46 — current 0x4F Item/Prop/binding Lua evidence and Go owner gates (2026-09-18)

## Authority and limits

Continue Stage45 HEAD `8e351a22c96fc8b5e5218d854255d514f3ef8da8`. Read-only current `lua64.package` SHA256 `700cb8c888374ed3ceb0f03b940e3f148620519f82352499f7957991184677d2`; exact decoded Lua 5.1 x64 chunk SHA256 `d40b705a8f4f2e5562879f6aa5a595dc4be3a9e5b64660279d8b311ac5b7ebe7` (26,500 bytes, 29 fully parsed prototypes; embedded source `@G:\Version\01_Client\lua64\form_stage_main\form_shop\form_exchange.lua`). Authenticated `exchangeitem.ini` SHA256 `ed931884d8a8bb19bad512b5eb571fe8ac97b783449144d4a4834eb050f0dee6` and `shop.ini` SHA256 `f42fce7631f7fe302d3fdedad60b12eaa20da12396de412ff0a84edcddc1a6d9` were obtained from hash-matched compressed resources, **not** a fully decrypted archive index. No raw Lua, EXE, DLL, package, key or other private client bytes are published.

## New correction: this Lua UI's guild Type is exactly 1

The Lua root prototype's debug locals `EXCHANGE_TYPE_NORMAL` and `EXCHANGE_TYPE_GUILDBUILDING` occupy R1 and R2. Root PC 25 (Lua line 19) loads numeric **0** into R1; PC 26 (line 20) loads **1** into R2. Root PCs 61–63 create the `update_consume` closure and capture R2 (`MOVE A=0 B=2 C=0`). Child `update_consume` has the named upvalue `EXCHANGE_TYPE_GUILDBUILDING`; PCs 133–142 (Lua lines 536–537) compare `nx_int(GetCurType())` to `nx_int` of that upvalue. Therefore **Type=1 triggers the guild-building *UI cost display* branch**, superseding Stage45's narrower statement that the numeric value was unknown. This proves neither official server-side Type semantics nor which ledger is charged. Type 2 and 3 meanings remain unproven here.

## Exact client bytecode flow (PCs zero-based per function)

| Lua function and declared lines | PC and Lua lines | Verified client action | Settlement boundary |
|---|---|---|---|
| `on_btn_exchange_click` 81–92 | PC 0–17, lines 83–88 | Converts `form.ipt_count.Text` with `nx_int`; when positive calls `nx_execute("custom_sender", "custom_exchange_item", form.shop_id, form.page, form.pos, count)`. | Not a server quantity, capacity, debit or grant validator. |
| `on_ipt_count_changed` 105–144 | PC 21–30, lines 121–126; PC 61–68, lines 140–143 | UI checks view object's `MaxAmount` when present, refreshes need-item and consume displays. | Server count cap unknown. |
| `update_need_goods` 261–347 | PC 42–55, lines 287–291; PC 63–89, lines 297–312 | `GetCurItemList()` is presented in `(ConfigID, number)` pairs (`table.getn/2`; slots `2*i+1`, `2*i+2` for zero-based `i`). Creates displayed `ArrayList` `ConfigID`, `Count`, `Amount`; both shown numbers are `get_rebate_value(viewobj, number * exchange_num)`. | Required item actual debit, stack/bind selection and VIP rules not proven. |
| `update_need_goods` same | PC 112–172, lines 323–344 | Fetches `GetCurBindStatus`, `GetCurExchangeBind`, `GetCurShowBind`. `BindStatus<=0` hides label; when positive it initially shows `ui_gpsd_bdfc_002`, positive ExchangeBind changes label to `ui_gpsd_bdfc_001`, and `ShowBind==0` hides it. | Label is not reward's actual bind algorithm or cost-item eligibility. |
| `update_consume` 485–551 | PC 30–60, lines 508–517 | `GetCurPropList()` shows `(property name, number)` pairs (`table.getn/2`, slots `2*i-1`, `2*i` for one-based `i`), displays `get_rebate_value(viewobj, nx_number(number)*nx_number(exchange_num))`. | No authoritative Prop state owner, mutation or rounding rule. |
| `update_consume` same | PC 64–131, lines 520–531 | `CapitalType1` and `CapitalType2` use format IDs `37402` and `37401`; other Prop names use `TextManager:GetText`, and formatted output is sent to `mltbox_consume`. | Display text is not a debit. |
| `update_consume` same | PC 133–183, lines 536–549 | This Lua's `Type==1` branch additionally renders `get_rebate_value(viewobj, nx_number(GetCurAddValue())*nx_number(exchange_num))` under `ui_guild_currency_9`. | Guild balance source, charge direction and reward semantics unknown. |
| `update_control_layout` 554–584 | PC 28–55, lines 573–582 | Uses Prop/Item list lengths to reposition UI boxes. | Not eligibility. |
| `isHaveItem` 587–596 | PC 11–27, lines 593–595 | `GoodsGrid:GetItemCount(nx_string(item_id))` comparison returns UI boolean and total; caller is item hover function lines 181–200. | Not the server buy handler or an inventory admission proof. |
| `get_rebate_value` 610–647 | PC 4–50, lines 614–646 | Client checks `VipModule:IsVip(VT_NORMAL)` and `VipRebate` view property; for numeric rate >0 and <=100 it applies rate/100 to its display value, then uses integer conversion and positive-fraction round-up on its final path. | Server VIP entitlement and charge arithmetic **not** established. |

This client Lua calls getters that expose the current S2C 557 config; it does not perform server debit, reward, persistence or replay handling. An empty Item/Prop cost display never proves a free grant.

## Population and 37-versus-38 disambiguation

The **entire** authenticated `exchangeitem.ini` has 10,585 sections and **38** distinct authored `Prop` names. Restricting to the authenticated `shop.ini` numeric page rows with `priceMode=3` and positive `ExchangeData` (the column offsets used by current `shop_catalog.go`) yields **42,936 listings**, **8,575 unique referenced ExchangeData**, and **37 referenced Prop names**. The 38th name, `GhostCityQBValue`, is present only in an unreferenced definition within this mode3 listing population; Stage45's 37 therefore had a narrower denominator. In the referenced population, **324 listings** refer to **138 distinct definitions with both Item and Prop**, **388 listings** have authored `BindStatus=1`; explicit `Type=1`: **2 listings**, `Type=2`: **41**, `Type=3`: **1,540**, omitted Type (default zero in Go loader): **41,353**. Nonempty AddValue is present on the explicit Type 1 and Type 2 listings; this correlation does not prove server direction or ledger. These are resource counts, not executable purchase cases.

## Actual current Go ownership cross-map; unresolved means BLOCK

- `playerActor` (`server/cmd/protocol-probe/player_actor.go`) owns `bagItems []bagItem` and monetary fields `silver`, `gold`, `silverCard`, `silverTicket` as `int32`. `stateProperties()` exposes **`CapitalType1` index 417 from `p.silver`** and **`CapitalType2` index 418 from `p.silverCard`**, both wire-encoded as `Int64Value`. This establishes client-facing state ownership only: it does not authorize any particular exchange debit or prove the field's saved database column.
- The other **35** referenced Prop names lack a complete independently verified current-Go chain of indexed property → actual role/player state → durable load/save → client update. Do not make up names, treat them as `silver`, or silently use a fake generic currency store. Likewise `Type=1` guild AddValue is a client UI branch without verified debit owner.
- `shopCatalogItem.configID` and `.amount` are authenticated listing metadata re-resolved from coordinates; no proven official equation relates listing `amount`, requested Count, displayed rebate quantity and reward creation.
- Stage45 source audit found `bagStore` writing `data/bag_items.json` (temp + rename), `currencyStore` separately writing `data/currency.json`, `mysqlBagStore.Save` and `mysqlCurrencyStore.Save` using **separate SQL transactions**; absent `NINEYIN_MYSQL_DSN` can select non-MySQL state. These are not one atomic Item + Prop + reward + role transaction. Exact item binding/capacity, signed range checks, crash rollback, duplicate request strategy, post-commit replication and reconnect must be proven for the actual runtime mode before enabling buys.

Read-only current-code anchors: [playerActor](https://github.com/kim8553/web/blob/8e351a22c96fc8b5e5218d854255d514f3ef8da8/server/cmd/protocol-probe/player_actor.go), [exchange config](https://github.com/kim8553/web/blob/8e351a22c96fc8b5e5218d854255d514f3ef8da8/server/cmd/protocol-probe/latest_client_shop_exchange_config.go), [0x4F contract](https://github.com/kim8553/web/blob/8e351a22c96fc8b5e5218d854255d514f3ef8da8/server/cmd/protocol-probe/latest_client_shop_exchange_contract.go), [bag/currency overlay](https://github.com/kim8553/web/blob/8e351a22c96fc8b5e5218d854255d514f3ef8da8/server/cmd/protocol-probe/zz_recovered_overlay.go), and [Stage45 storage boundary](https://github.com/kim8553/web/blob/bfea8bf6128bfab0787861f3e22991deb4539db4/recovery/CURRENT_EXCHANGE_0X4F_STAGE45_NATIVE_GETTER_XREF_AND_STORAGE_BOUNDARY_20260917.md).

## Validation and next step

**PASS, local static only:** matched decoded Lua chunk SHA256, consumed 29 bounded prototypes, verified exact Type=1 lexical capture, getter/bind/rebate constants, matched both current INI digests and classified referenced rows; read actual current Go actor/config/handler files through authenticated GitHub connection. **NOT RUN:** new server Go/Windows build, SQL atomicity or rollback/concurrency, server boot, live 0x4F buy, persistence/reconnect. No production handler, DB schema or client bytes were changed. **0x4F remains FAIL-CLOSED.**

**Next single priority:** trace the other 35 referenced Prop names to exact current player state and durable role-store read/write/replication owners, establishing a semantics-proven subset before designing or enabling a single-transaction settlement. Preserve all unsupported combinations as blocked.