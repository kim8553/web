# Stage48 — normal item / NPC retail route and purchase-total guard (2026-09-18)

## Scope and authority

Continue Stage47 commit `4bcc6135439ea945a12e3ae011ecaf6f2285aedb` on `kim8553/web` branch `stage37-assistant-handoff-20260917`. User priority is common item operations, regular NPC purchase/sale; **GM grant and exchange purchase 0x4F are explicitly deferred and were not changed**. Existing exact-current client Lua64 was decoded read-only from the already hash-pinned package, using the EXE-derived key privately; only SHA256, Lua source identities, function line locations and packet layouts are documented. No private source chunk, DLL, EXE, package or key was pushed.

## Exact-current client request versus current Go routes

Current decoded `@G:\Version\01_Client\lua64\form_stage_main\form_shop\form_shop.lua` SHA256 `0f014f45f7e36c14170d30757b84bb4d462133401991e121f9be29c05716e54a` (37,804 bytes; statically parsed 38 complete Lua 5.1 x64 prototypes): `custom_sell_item` is referenced at Lua lines 683 and 716; `custom_buy_item` at lines 761 and 812. This establishes client calls, not authoritative payout or grant.

Current decoded `custom_sender.lua` SHA256 `eb0fda812d9c2db8d954ca1b7c5f98c998e17a8ff1c9568216ea4ac3266bfe6b` (394,078 bytes, 798 parsed prototypes): function lines 189–196 emits `CustomSend(CLIENT_CUSTOMMSG_BUY_FROM_SHOP, nx_string(shopID), nx_int(page), nx_int(pos), nx_int(count))`; function lines 199–206 emits `CustomSend(CLIENT_CUSTOMMSG_SELL_TO_SHOP, nx_int(srcView), nx_int(srcPos), nx_int(count), nx_string(shopID))`. Current decoded `share/client_custom_define.lua` SHA256 `2caca2884720f12976a31613425ea70b4f745b131e30149e26f443ae1e0f0ac3` assigns **buy 70 (0x46)** at line 96, **sell 71 (0x47)** at line 98, buy-back 73 at line 100, and exchange 79 (0x4F) at line 102. These field shapes must not be confused with exchange 0x4F. The current Lua `SellPrice1` read at form_shop.lua lines 585/650 is UI display, **not proof of official server sale payout or price calculation**.

Actual checked-out Go `server/cmd/protocol-probe/main.go` case 70 calls `handleShopBuyCustom`; cases 30/31/34/36 call bag move/use/delete/arrange. In inspected dispatch around lines 1792–1855, **no case 71 sale handler is present**; the [Stage48 route scan](https://github.com/kim8553/web/actions/runs/35240064625) provides further bounded source-discovery evidence, not a proof of absence in every external service or runtime. `server/cmd/protocol-probe/zz_recovered_overlay.go` defines `handleShopBuyCustom` at approx lines 6470–6560.

Hash-pinned current `shop.ini` SHA256 `f42fce7631f7fe302d3fdedad60b12eaa20da12396de412ff0a84edcddc1a6d9` numeric rows by authored price mode: **4,120 mode1, 694 mode2, 42,985 mode3**. The **4,814 mode1/2 rows** are an *authored listing population*, not evidence that all are implemented or purchaseable. Two mode2 listings have `price*99 > maxInt32`: `box_baohe_003` (30,000,000 per row) and `qy_qiegao_item001` (1,600,000,000 per row). The prior handler first checks the available actor balance, so this inventory audit **does not claim an executed overflow exploit**.

## Production change and exact verification

The original `zz_recovered_overlay.go` blob `1700ac2889273ca806494916ddbc6efcf6145f29` computed `int64(price)*int64(amount)` but cast the result to `int32` at each currency mutation without an explicit representability/negative-price guard. Introduced `latest_client_normal_shop_buy_guard.go` with `normalShopSafeTotal(price, quantity int32)`: reject price <0, quantity <1 or >99, or total >2,147,483,647; return an int64 total and bool. A bounded patch script `tools/stage48_normal_shop_guard/apply_normal_shop_total_guard.py` pins the exact old blob and the sole purchase anchor, replaces only the buy total computation with the helper and an early refusal, and touches no GM, exchange or seller code.

[Stage48 CI run 35241308367](https://github.com/kim8553/web/actions/runs/35241308367) completed SUCCESS: `gofmt`/hash-pinned one-file diff check, local decoder-shim compile, **12 positive/negative total-boundary unit subtests under `go test -race`**, whole current protocol-probe **Go test binary compile** (`go test -c`) and Linux server executable **Go build** without running resource-dependent package init, then commit/push only the updated overlay. The resulting production overlay Git blob is `15568d3f43794eb78045a39655e1a4964b8fb736`; [applied source commit `65e840d16538053722c312f91013677256bb824b`](https://github.com/kim8553/web/commit/65e840d16538053722c312f91013677256bb824b) was independently read back at its commit and confirmed to be the branch HEAD before this report. Neither the compilation nor isolated arithmetic test proves real buy behavior.

Preceding CI attempts are not omitted: run 35240579664 failed full-package compile at three pre-existing `decoder.String` calls, because the checked-in `server/_builddeps/xtext/encoding/simplifiedchinese` dependency is an **identity-only placeholder** missing `String`. Added its byte-preserving compatibility method and gofmt in commits `1fdaf041...` and `01c6c3b5...`; this fixes compile ABI only and **does not decode GBK correctly**. Run 35241010374 then compiled but could not execute package initialization because the public checkout lacks `server/resources/modern/share/skill/skill_new.ini`: no fake resource was added. Run 35241134539 passed isolated tests/full compile but failed strict publish cleanliness because `python -m py_compile` wrote an untracked `__pycache__`; CI now places Python bytecode in runner temp and verifies a one-file diff before commit. The final success supersedes the failed attempts in its strictly limited test scope.

## Remaining blockers / no completion claim

The current buy handler mutates actor currency, adds an item, sends client frames and separately persists bag and currency; `persistBagEquip` and `currencyStore.Save` do not form one durable atomic purchase. Persistence errors can be logged rather than causing a rollback. Listing quantity versus reward amount, item stack limit, bag-space checks, verified currently opened NPC shop session for normal purchases, correct item art/type/effect, full GBK names, duplicate purchase handling, reconnect and client replication remain unproven. Current sell request **0x47 exists client-side**, but official sale price, allowed item/bind categories, payout ledger, buy-back, atomic item removal+currency grant, and current Go seller handler are not verified or implemented. No arbitrary sale formula or fictitious handler was inserted.

**Status:** common item routes are partially present; one **normal purchase arithmetic guard is committed, isolated tests PASS, Linux server compile PASS**; no Windows build, runtime server boot, real normal buy/sell, SQL rollback, persistence/reconnect or complete-item test ran. GM grant and 0x4F exchange remain deferred; 0x4F stays fail-closed.

**Next single priority:** establish the current normal shop purchase **atomic bag + currency persistence and rollback boundary** for the actual selected JSON/MySQL runtime, before representing normal buying as safe. In parallel only if independently evidenced, identify the sale-price authority for client 0x47; never synthesize sale payouts from a UI label.
