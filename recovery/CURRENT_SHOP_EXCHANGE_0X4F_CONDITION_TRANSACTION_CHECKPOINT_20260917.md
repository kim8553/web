# Current-client C2S 0x4F condition/range/transaction checkpoint — 2026-09-17

## Authority and execution

- Continue `kim8553/web` branch `stage37-assistant-handoff-20260917`. Baseline before this continuation: `5d6c671ae97d971be9ae71adbf95607d74b7d322`. The source-audit workflow commit was `37decd19c7b4fc3a0e30bb393bfb23e10c31d084`; the read-only condition-observation source commit was `c34ef9d2fe7f5a6215a3f680138589f0e0c3822a`. This document is a subsequent checkpoint, not a rollback target.
- Server lineage remains `9yin-go-server1.rar`, while current Go source is GitHub. Client binary/resource authority remains Google Drive `bin64.zip` and `res` per `CURRENT_SHOP_EXCHANGE_0X4F_EXECUTION_AUDIT_20260917.md`. No newly extracted inner bytes from the opaque Drive PCK0 packages or new live packet observations were obtained here.
- Reproducible source search: `.github/workflows/jiuyin-exchange4f-evidence-audit.yml`, run https://github.com/kim8553/web/actions/runs/35220784630 (SUCCESS). Search covers actual working-branch `server/cmd/protocol-probe/*.go`; individual hits are investigative leads, not proof that a listed radius is the shop's native radius.

## Actual implemented change: read-only condition observation

- `latest_client_shop_exchange_condition_observation.go` summarizes root count, `AllSupported` and number of `UnsatisfiedOrUnresolved` roots. It has no permission/grant field, no mutation, and explicitly treats zero roots as *not* purchase authorization. `latest_client_shop_exchange_condition_observation_test.go` checks all-true, unmet, unsupported, empty and repeated non-mutating cases.
- `latest_client_shop_exchange_contract.go` now invokes the *existing* `exactCurrentConditionEvaluator` and `evaluateExchangeConditionDetails` (the same established 508 root path) for a syntactically valid, session-bound, SAFE 0x4F request. It logs the resulting counts and **always returns without debiting, granting, saving, or sending an invented result**. A nil player/catalog or evaluation error remains blocked. This does not assert native purchase semantics for `Condition` vs `Condition2`; multiple player fields are read under separate locks and do **not** form a coherent transactional snapshot.
- Pinned source-replacement script `tools/stage37_exchange4f_condition_observation_patch.py` insists on exact production blob `35add8545f7a006045c231c237c49b47be98bce1` and one exact anchor. `.github/workflows/jiuyin-stage37-exchange4f-condition-apply.yml` tests and then fast-forwards only the contract source edit, with no force push or production go.mod replacement.
- Verified CI: https://github.com/kim8553/web/actions/runs/35220999087 — SUCCESS. Standalone production summary tests and race PASS; existing production-extracted 0x4F regression/race PASS; session guard/race PASS; other package tests PASS; protocol-probe test binary **COMPILE** PASS; `go vet` PASS; Windows amd64 PE32+ cross-build PASS. Full protocol-probe runtime suite, real client LIVE, actual exchange and reconnect E2E **NOT RUN** because external exact-current resource files are not in GitHub checkout. A build is not a live pass.

## Exact interaction-distance evidence and unresolved boundary

- `zz_recovered_overlay.go` `playerActor.updatePosition` (around lines 4359–4367) tracks position under player mutex; `authoritativePosition` (around 4370–4375) snapshots this server-tracked position. `scene_lifecycle.go` stores spawned NPC `sceneEntity.transform` under scene mutex, while `currentShopExchangeSessionAllows` checks same NPC/config/owner/shop/scene epoch and service advertisement.
- `main.go` `synchronizeNPCViewport` (around lines 1998–2014) uses configurable `npc-radius` **for visibility**, default 120 in the CLI; `combat_ai.go` `npcWakeupRange` is combat aggro; `door_routes.go` `portalTriggerRadius` and `SpringRange` concern portals. **None of these is established as the exact-current shop interaction radius.** Do not reuse 120, WakeupRange, SpringRange or combat skill range for exchange permission. No arbitrary distance threshold or expiry was introduced. The source search does not prove the absence of a native rule hidden in current client binaries/packages.
- Before any mutation: recover version-matched current Lua/native NPC-shop interaction reach and coordinate/frame semantics, then validate the player remains within that proven range at the final serialized commit boundary. Existing session ownership is necessary but not a purchase authorization.

## Bag / DB source audit: exact scope of existing transactions

- `zz_recovered_overlay.go` `mysqlBagStore.Save` (around lines 3483–3510) uses one `sql.DB.BeginTx`, deletes `role_bag_items` for a role, inserts the supplied bag snapshot, then commits; rollback is deferred on error. **This proves only a single bag-table snapshot rewrite transaction in that function**, not a coupled material debit+reward exchange, no cross-store atomicity, no idempotency, and no concurrent stale-snapshot protection. A transaction that commits an incorrect or stale snapshot is still incorrect. It is not currently called by C2S 0x4F.
- `mysqlBagStore.Load` (around lines 3451–3481) reads the same role's bag and returns `len(items) != 0` as its second boolean. The semantics for an intentionally empty bag versus a missing store therefore need review before reconnect tests; do not assume this boolean means `loaded successfully` for an empty bag.
- `bagItem` (around lines 4212 onward) carries additional runtime/JSON fields (`ColorLevel`, `MaxAmount`, `FuncPack`, `LogicPack`, `PropModifyPack`, `TextureType`, `CardID`, `ToolUseEffect`, `FuncBuffer`, damage fields), but `mysqlBagStore.Load`/`Save` select/insert a narrower set (`config_id`, `item_type`, `amount`, `view_id`, `name`, `equip_type`, `art_pack`, `hardiness`, `max_hardiness`, `slot`, plus role and sequence). **No lossless round-trip of those additional properties is demonstrated by the current MySQL code.** Do not invent DB columns or treat item binding as persisted without exact schema/resource proof.
- JSON fallback `bagStore.Save` (around lines 276–313) updates its in-memory role snapshot under a store mutex, writes the JSON to a `.tmp`, and renames it, restoring the previous in-memory entry on write/rename errors. It is an independent bag-file operation, not a two-resource durable transaction. Crash durability and duplicate-request behavior were not demonstrated.

## Next strict unfinished work

1. Obtain a version-matched authoritative NPC shop interaction range from current `bin64.zip`/`res` and trace server movement/teleport updates. Do **not** turn viewport, portal or aggro ranges into a shop distance guess.
2. On the exact-current ExchangeItem/Item/Prop definitions and player bag state, prove which resources are materials vs results, multiplicity/overflow, binding partition and consumption order. `ShowBind`/`ExchangeBind`, 388 positive BindStatus rows, and undocumented `DropBind` interpretations remain blocked. A condition observation is not eligibility.
3. Build **tests first** around a per-role serialized, rollback-safe debit+grant operation with failed insert/commit, repeated request, full bag, insufficient materials, concurrent requests, postcommit view update and reconnect. Use the existing real DB schema/columns only; verify that all required bag item fields round-trip or keep such exchanges blocked. Revalidate session/resource/state inside the final atomic boundary rather than trusting separate preflight reads.
4. Full protocol-probe runtime tests on exact-current resources, Windows client LIVE and reconnect E2E are outstanding. No actual item grant/debit was enabled in this checkpoint.
