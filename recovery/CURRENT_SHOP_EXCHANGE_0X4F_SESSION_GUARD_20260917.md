# Current-client C2S 0x4F shop-session preflight checkpoint — 2026-09-17

## Authority and changes

- Working repository/branch: `kim8553/web` / `stage37-assistant-handoff-20260917`. Prior checkpoint `05b62385b29534c1e553c5e840d1d33e14749e7e`. Production source patch committed as `94e73bc70e76248763fea29a87517af5318c022c`; neither reset nor source substitution was used.
- Original server lineage remains `9yin-go-server1.rar`. Exact-current client remains Google Drive `bin64.zip` and `res` as recorded in `CURRENT_SHOP_EXCHANGE_0X4F_EXECUTION_AUDIT_20260917.md`; no new extraction or digest of opaque PCK0 package internals was performed in this phase. Historical extracted INI hashes remain pre-existing authority claims, not newly reverified against package internals here.
- `server/cmd/protocol-probe/latest_client_shop_exchange_session_guard.go`: a conservative local server-state record and a pure identity matcher. No new wire fields or upstream session protocol are claimed.
- `server/cmd/protocol-probe/latest_client_shop_exchange_session_lifecycle.go` and `scene_lifecycle.go`: record the NPC ID, owner, config, shop ID and scene epoch only after the NPC's shop service successfully sends its shop view. Reject a session whose NPC disappeared or no longer advertises the same ShopID. Clear the record on scene begin, NPC unregister, another object interaction, or another service attempt. All scene inspection uses the existing `sceneLifecycle.mu`.
- `main.go`: dispatch C2S 79 separately from 64/69, parse the already-known five typed request values, require a non-nil player and a matching opened NPC shop session before invoking the existing read-only exchange handler. Forged ShopID, no prior shop, stale scene, unregistered/reused NPC, and service mismatch fail closed. The existing 0x40 form and 508 display paths remain separate.
- `server/cmd/protocol-probe/latest_client_shop_exchange_resource_recheck.go` and `latest_client_shop_exchange_contract.go`: SHA256-verify the five already-known exact-current authority inputs on each authorized 0x4F preflight, before and after resolution. A `sync.Once` cached authority is not enough if resources change later. Recheck introduces no invented INI schema or hardcoded binding value.
- Added `latest_client_shop_exchange_session_guard_test.go` and source-extracted actual production fingerprint checker tests under `tools/stage37_exchange4f_hash_*`; pinned source blob/anchor patch script and `.github/workflows/jiuyin-stage37-exchange4f-session-apply.yml` apply only three verified source edits after CI checks, then fast-forward push without force.

## Executed validation

- GitHub Actions run https://github.com/kim8553/web/actions/runs/35220233398 finished **SUCCESS** on its checked-out sources. The log shows the exact-source patch applied, session identity cases PASS, changed/missing resource fingerprint rejection PASS, existing 0x4F isolated purchase-preflight tests PASS, isolated race tests PASS, other Go packages' tests PASS, protocol-probe test binary compile PASS, `go vet` PASS, Windows amd64 PE32+ build PASS, and commit/push of `94e73bc70e76248763fea29a87517af5318c022c` PASS.
- The main protocol-probe runtime test suite/full `go test ./...` was **NOT RUN** because latest client external resources including `modern/share/skill/skill_new.ini` are absent from the GitHub checkout. A test-binary compile is not a runtime pass. A compiled Windows binary is not a successful client connection. Latest-client LIVE, actual exchange, MySQL/JSON transaction, item sync and reconnect E2E are **NOT RUN**.
- A legacy mode3 workflow still compares `server/` to the historical handoff tree `70083784...` and fails its historical equality gate on any new server file. That obsolete SHA gate is **not** a gameplay-regression result and must not be called PASS. Use the above exact-source CI result for this checkpoint.

## Explicitly unresolved — do not activate debit/grant

1. **Proximity/range:** NPC ownership and service selection are now checked, but an exact-current authoritative interaction radius and proof of the current player staying within it are **not established**; the matcher intentionally invents no distance threshold. The read-only preflight is not purchase authorization.
2. **TOCTOU/resource snapshot:** Two SHA256 passes detect ordinary on-disk changes between invocations, but files are not captured atomically, and scene-session check and subsequent purchase processing are not one transaction. Before any inventory mutation, prove immutable/versioned current resource snapshots and serialize the final session/condition checks with the transaction.
3. **Conditions/materials/binding:** SAFE evaluator capability does not prove the player's condition acceptance. Material identities, quantities/count multiplication overflow, bound/unbound debit sequence, result binding, capacity/stacking and authoritative 557 ShowBind/ExchangeBind remain unresolved. The 388 positive BindStatus entries remain fail-closed.
4. **Persistence and postcommit wire:** No coupled debit/grant, single durable DB commit, rollback, duplicate-request strategy, bag/view replication or reentry E2E exists for 0x4F. Existing independent bag/currency stores are not evidence of atomic transaction semantics.
5. **Latest resources:** `res/ini.package` and `res/share.package` are opaque PCK0 files; internal extraction and exact version matching of `shop.ini`, `ExchangeItem.ini`, `tool_item.ini` remain unverified here. No missing-file inference is made.

## Next exact continuation

Investigate the already-present scene movement/viewpoint/interaction coordinates and exact current client evidence for NPC shop interaction range *without guessing a radius*. Independently inspect the existing condition evaluator with a player snapshot and the exact-current exchange definitions to identify a demonstrably safe material/result subset. Before any grant/debit, design and test a single rollback-safe bag/DB operation, idempotency, failure response and reconnect persistence. Keep 0x4F item mutation disabled until ALL required evidence and LIVE/E2E tests are available.
