# Stage34: authored shop pages / PageCount consistency

Authority: user's `9yin-go-server1.rar`, SHA-256 `ddc2f6bc078660a40eedf077d43befd6402ed59478ccde987d44171c1d7aefa5`; exact extracted `resources/modern/share/trade/shop.ini`, SHA-256 `ceb7a3a7e17cbc118ce5a7d3d979dea6120c50cdf6a5823697c05a5b1b9cf5dd`. Existing GitHub branch: `stage37-current-recovery-20260916`, Stage32 reconstruction plus Stage33's offline audit and separate diagnostic launcher. Do not reset the branch or replace the RAR/old EXE.

The RAR's `Shop_special_001` has `PageInfo=Page1` but two authored product entries at numeric keys 0 and 1. Prior production `loadShopCatalogSection` returns 2 rows and `PageCount=1`. The Stage34 patch uses `max(existing PageInfo count, maximum authored numeric page+1)` without adding items, changing their prices/coordinates, or touching files. Stage34 patch SHA-256: `5da04ccd051db7cc27a6205db789939a5f0d65a3bd34797a3c2fc73a616fb280`; patched `shop_catalog.go`: `b683cfcf00a1b6b2d3bf1b37aceb266f2555e82fa91a181bdeb9bc702c1a8e51`.

Local verification using the **unmodified and patched actual Go parser** on all 1,447 RAR shop sections: exactly one changed section, `Shop_special_001`, `PageCount 1 -> 2`. All item hashes and all empty-shop errors unchanged. Three new isolated tests and five retained Stage32 tests, including exact private RAR fixture, passed; isolated race passed. Using the actual current `currentShopViewObjectIndex` and Stage31 preflight code locally, the two authored product slots preflight at object indices 1 and 501 without collision. Real client render and usage of the dialog-option route are not proven. The shop is referenced in `rule/optiontalk_dialog_pack.ini`; it was not among city05 or born02's direct NPC ShopID audit entries.

CI workflow: `.github/workflows/stage37-stage34-shop-pagecount.yml` running `tools/stage37_current_recovery_stage34.sh`; it reconstructs Stage32, verifies all inputs by hash, applies only this patch, runs isolated/race, compiles full package and full race test binary (does not execute them), runs `go vet`, and builds Windows amd64. GitHub CI has no user's private RAR resources, so the exact RAR test is skipped in CI but was executed locally. Check run ID `35093960024` for latest result before reporting a build PASS.

Preservation: Stage29 known born02 coordinates and Lua ordinal remap unchanged; Stage30 exchange DISPLAY remains opt-in and OFF by default; Stage31 full-frame preflight unchanged; Stage32 NPC selection diagnostics unchanged; all ordinary and exchange purchases remain fail-closed when exact current wire/final binding authority is missing. GM web item grant deferred. No user's PC binary, DB, or original RAR changed. LIVE/E2E not run.

Next high-value external boundary: actual latest client selection trace from the safe Stage33 diagnostic kit, connecting NPC config -> ShopID -> emitted View61/item frames -> client-visible items and the exact C2S purchase event. Do not interpret an offline shop parser or executable build as game-level success. Do not map `Shop_GB_Yishiting` to any numbered suffix without a verified rule.
