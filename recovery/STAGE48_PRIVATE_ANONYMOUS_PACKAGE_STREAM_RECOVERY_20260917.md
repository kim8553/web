# Stage48 — 원본 PCK0 익명 zlib 스트림 복구 (2026-09-17 KST)

## 권위·보존·범위

- 시작 HEAD: `c7ba33de6b72ae5516613725b812951cc74e5b55` on `stage37-current-recovery-20260916`. 기존 `9yin-go-server1.rar` 기반 `server/`는 유지하며 `9yin-go-server.zip`이나 옛 JYZJ 소스로 교체하지 않음.
- Stage39–47에서 확인한 **원본 Drive 패키지 사본** `ini.package` SHA-256 `6185812c6153b2a6da56071968dcd1254510a974df06a7e36a0229be2105779a`, `lua.package` SHA-256 `283c8c245a3fb86af2a7e8c21c53b3de26c30432341dd590c37554adfce04e97`만 대상으로 함. 실제 패키지 크기·SHA·PCK0 헤더·선언 엔트리 수·인덱스 끝을 모두 재검증한 뒤, **인덱스 끝 이후 데이터 영역**만 검사.
- 새 공개 도구 [`tools/stage48_anonymous_zlib_recovery.py`](../tools/stage48_anonymous_zlib_recovery.py)는 **일반 zlib 헤더 + 완성된 압축 스트림 + Adler-32 검증**을 만족하는 익명 스트림만 복구함. 압축/해제 각각 4 MiB, 총 출력 256 MiB, 최대 30,000 스트림 등의 한도를 둠. 인덱스 해독·원본 파일명 부여·클라이언트 EXE 실행·게임 서버 수정 기능은 없음.
- 실제 개인 데이터가 담긴 출력 ZIP과 각 원본 바이너리 및 게임 리소스 바이트는 **GitHub·Actions 아티팩트에 일절 업로드하지 않음**. 공개 저장소에는 파이썬 일반 도구·합성 CI·이 통계 보고서만 저장. 출력 ZIP은 해당 대화의 비공개 `/mnt/data/STAGE48_ANONYMOUS_STREAMS_PRIVATE_20260917.zip`에만 보관하며, 공개 URL을 만들지 않음.

## 검증된 익명 스트림 관측값 — 원본 파일 목록 아님

| 원본 패키지 | 헤더에 선언된 인덱스 엔트리 수 (해독 전) | 완전한 zlib 스트림 검증 수 | 압축된 스트림 바이트 합 | 해제 후 바이트 합 | 스트림 사이 미분류 영역 개수 / 합 | 마지막 스트림 뒤 미분류 바이트 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `ini.package` | 18,543 | **18,567** | 11,557,726 | 81,433,325 | 811 / 7,752,310 | 49,146 |
| `lua.package` | 2,286 | **2,295** | 21,107,543 | 36,572,036 | 29 / 434,445 | 110,921 |

- 두 패키지에서 유효한 익명 스트림 **20,862개 / 해제 후 총 118,005,361바이트**를 확인. 원본 패키지의 선언된 엔트리 수보다 zlib 스트림 수가 각각 24개와 9개 더 많음. 한 인덱스 항목과 한 스트림의 1:1 대응을 **전제할 수 없음**. 스트림 사이와 끝의 미분류 영역에는 다른 저장 형식, 압축되지 않은 데이터, 미참조 스트림 등이 있을 수 있으나, 어느 것인지 아직 모름.
- 모든 유효 zlib 헤더 패턴(사전 딕셔너리가 없는 일반 zlib)을 추가로 조사했지만, 발견된 완전한 비중첩 검증 스트림은 위의 `0x78 0x9c` 스트림과 동일했음. 이는 **이 검사 기준에서만** 다른 완전한 헤더를 찾지 못했다는 의미이며 다른 패킹 방식의 부재 증명은 아님.
- 인덱스 복호화 **0건**, 인덱스 레코드 식별 **0건**, 원본 파일명에 연결된 패키지 멤버 **0건**. 실제 `shop.ini`나 Lua 경로를 확인했다고 보고하지 않음. 전체 언팩 / 신규 버전 동기화 / LIVE·E2E는 실패가 아니라 **미수행·미확인** 상태.

## 개인용 출력 ZIP 무결성

- 생성 파일 `STAGE48_ANONYMOUS_STREAMS_PRIVATE_20260917.zip`: 36,655,696바이트; SHA-256 `42665ddc3b74b92a4be1b06fa07aeb316fcdcec5f07a0ec1b46da12ee54f581d`; ZIP `testzip()` CRC **PASS**.
- 내부 익명 데이터 20,862개 + `manifest.json` + `README_KO.txt` = 총 20,864 ZIP 엔트리. `manifest.json`은 각 `ini/stream_...bin` / `lua/stream_...bin`에 원본 바이트 시작/끝, 해제 크기, SHA-256을 연결함. 첫/마지막 INI·Lua 표본 4개의 추출 크기·SHA를 독립적으로 재확인해 전부 일치. 파일 이름은 **임시 익명 식별자**이고 게임 클라이언트 경로가 아님. 게임 폴더에 덮어쓰거나 임의로 `shop.ini`로 이름 바꾸지 말 것.

## 이미 연결된 Google Drive 상점 자료와 비교

- Drive `resources/modern/share/trade`에서 기존 `shop.ini` (2,176,439 bytes), `shopfunc.ini` (116,512), `shopsingle.ini` (3,693), `clonestore.ini` (1,066,765), `repairaddbag.ini` (577)의 원본 바이트를 **개인 작업 공간에만** 읽어 SHA-256을 비교. 이 다섯 파일과 이번 **검증된 단일 익명 스트림** 사이에 완전히 같은 SHA는 **0건**.
- 여기서 *원본 패키지에 상점 파일이 없다*, *구형 자료와 다르다*, *업로드된 최신 BIN64와 패키지 빌드가 일치한다*는 결론은 낼 수 없음. 미분류 영역, 파일 표현 방식, 판본 차이 및 파일명-스트림 대응은 확인하지 못했음. Stage44–47의 상점 readiness 불허 판단은 그대로 유지.

## 코드 및 검증 수준

- 새 도구 로컬 `python -m py_compile` **PASS**, 합성 자체 테스트 **PASS**: 두 zlib 헤더 변형, 스트림 사이 간격, truncation/Adler-32 손상/압축 해제 상한/압축 입력 상한/빈 입력 거부. 실제 두 패키지에 SHA 핀 적용 후 복구·출력 ZIP CRC **PASS**. GitHub blob SHA `9a882ab868a4530a22fa11b7d17f99161977f622`는 실행했던 로컬 스크립트의 `git hash-object`와 일치.
- GitHub Actions [Stage48 synthetic-only run 35118902656](https://github.com/kim8553/web/actions/runs/35118902656): `synthetic-only` job, Python 문법 및 합성 테스트 단계 모두 **SUCCESS**. **CI에는 게임 파일을 넣지 않았으며**, CI 성공은 실제 패키지의 인덱스 언팩 또는 상점 기능 정상화를 뜻하지 않음.
- **Go gameplay/GM 지급 코드 변경 없음; 신규 Go build, race, vet, 게임 실행/로그인, NPC 구매·가방·DB 지속성 LIVE/E2E 실행하지 않음.** 기존 `PurchaseReady=false` 유지.

## 다음 증거 경계

실제 동일 `fxres.exe`/`fxpackage.dll`에서 *인덱스 변환과 파일명-오프셋 결합을 수행하는* 확인된 코드 경로를 확보해야 전체 파일 목록을 복원할 수 있음. 익명 스트림 SHA와 이름이 확인된 최신 동판본 자료가 있을 때만 특정 이름에 결합하고, 정확한 상점 INI/Lua와 구매 패킷·화폐·가방/DB 계약이 확보되기 전에는 구매 로직을 추측 수정하지 않는다.
