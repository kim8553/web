구음진경 최신 Snail 클라이언트 + 9yin-go-server1 Go 소스 기반 복원 작업을 이전 채팅의 Stage36 최신 상태에서 그대로 이어서 진행해.

첨부한 `JIUYIN_SOURCE_BASE_STAGE36_HANDOFF_20260912.zip`을 최신 인계 기준으로 사용해. 내부 `HANDOFF_MANIFEST.txt`의 Git commit과 SHA256을 먼저 확인하고, `repo.bundle` 또는 `source_snapshot.tar.gz`에서 작업 상태를 복구해.

반드시 지켜:
- 최신 Snail current client `fxgame.exe / FxGameLogic.dll / fxnet2.dll / fxcore.dll` 및 동일 current Lua/resource만 프로토콜/행동 의미의 권위 기준으로 사용.
- current 서버 차이 복원은 exact `fee2df...` 9yin-game-native-menu.exe의 DWARF/기계어를 기준으로 사용.
- `9yin-go-server1`은 editable 구현 몸통이다. 옛 서버 gameplay semantics를 최신 기준으로 착각하지 마.
- V37/V46/JYZJ/V2 과거 동작을 최신 권위로 섞지 마.
- 추측 수정 금지. 불확실하면 `확실하지 않음`, 모르면 `알 수 없습니다`.
- STATIC PASS와 live/E2E PASS를 반드시 구분.
- `.recovered` 파일 통째 덮어쓰기 금지. 일부는 current EXE에서 복원된 함수 조각뿐이다. 반드시 함수 단위로 병합.
- 이미 완료된 Stage1~36 분석을 반복하지 마.
- gameplay behavior patch는 근거 확보 전 임의로 넣지 마.

현재 핵심 상태:
- current fee2df DWARF source path: 87/87 확보.
- Stage36 함수 단위 merge inventory: current named source functions 739개 중 674개 baseline/recovered 대응, 65개 추가 확인 필요.
- 다음 작업은 `cmd/protocol-probe/equip_wear.go` missing 20개 함수의 함수단위 current-EXE 대조/분류/병합 감사다.
- 그 다음 `scene_lifecycle.go` 13개, `shortcut_records.go` 8개 순서.

먼저 `docs/HANDOFF_STAGE36_20260912_KO.md`와 `evidence/stage36/function_merge_inventory.txt`를 읽고, `equip_wear.go` 20개부터 계속 작업해. 사용자에게 다시 파일을 요구하거나 처음부터 분석하지 말고, 인계 상태에서 바로 이어가.
