# 최신 클라이언트 Lua 두 패키지: NPC 판매 요청 조사 후보 축소 (2026-09-18)

## 출처·확인 단위

- 구현 원본 계보: 사용자 제공 `9yin-go-server1.rar` (SHA-256 `ddc2f6bc078660a40eedf077d43befd6402ed59478ccde987d44171c1d7aefa5`); 실제 Go 수정 기준: `kim8553/web`의 `stage37-restored-20260918`. 작업 시작 시 HEAD `8569bc8f869bf6377c9f8969223eb9a8584758f1`.
- 직접 연결된 [Drive `res/lua.package`](https://drive.google.com/file/d/1jMfaMUsM5EhLo4hG-zo_3OamgpHVoMt6/view) (21,840,086 bytes, SHA-256 `283c8c245a3fb86af2a7e8c21c53b3de26c30432341dd590c37554adfce04e97`), [Drive `res/lua64.package`](https://drive.google.com/file/d/1NWh8NSeN-mQ2RdeJi9yCkX40uuDBW7j6/view) (21,956,023 bytes, SHA-256 `700cb8c888374ed3ceb0f03b940e3f148620519f82352499f7957991184677d2`). 원본을 각각 읽기 전용으로 복사해 실제 바이트를 해시로 확인했다. 폴더 안에 두 파일이 존재한다는 사실만으로 최신 게임이 어느 패키지를 우선 사용하는지는 정할 수 없다.
- `PCK0` 헤더의 엔트리 수 필드는 두 패키지 모두 **2,286**이다. 파일 오프셋 `0x0e`의 4바이트 필드 값은 각각 **187,177**, **191,749**이며 해당 오프셋에서 유효한 zlib/LuaQ 스트림이 시작한다. 이 수치는 디렉터리 파일명·스트림 연관 관계의 판독 결과가 아니다.

## 실제 바이트로 재현한 관찰

`server/tools/audit_client_lua_sale_candidates.py`는 입력 파일 SHA-256이 예상값과 다르면 내용 조사를 거부한다. PCK0 헤더 오프셋 이후의 zlib 후보를 **용량·재귀 깊이·배열 길이 제한**을 두고 검사하고, 전부 끝까지 구조를 읽은 LuaQ/Lua 5.1 바이트코드 스트림만 센다. 두 패키지에서 관찰된 `abcd` 4바이트 스칼라 마스크와 64비트 `size_t`의 `abcd464f` 마스크로 **문자열 상수의 첫 4바이트**만 후보 비교에 사용한다. 전체 문자열, 코드 명령의 의미, 클라이언트 송신 경로는 복호화·해석하지 않는다.

| 항목 | `lua.package` | `lua64.package` |
|---|---:|---:|
| 구조를 끝까지 검증한 독립 LuaQ zlib 스트림 | 2,294 | 2,292 |
| 앞 4바이트가 `sell`인 문자열 상수가 포함된 스트림 | 61 | 60 |
| `sell` + (`cust` 또는 `send`) 앞부분이 함께 있는 스트림 | 47 | 47 |
| `sell` + `shop` 앞부분이 함께 있는 스트림 | **3** | **3** |

이 수치는 **구조적으로 식별된 독립 압축 스트림 수**이지 PCK 색인에 등록된 파일 수, 서로 다른 스크립트 수, 실제 게임에서 호출되는 판매 핸들러 수가 아니다. 따라서 검출 스트림 수가 헤더 선언 엔트리 수보다 많아도 패키지에 사용되는 파일이 그만큼 있다고 단정하지 않는다. `sell`, `shop`, `cust`, `send`는 **상수 앞 4바이트의 부분 일치**일 뿐 완전한 함수명이나 호출 관계가 아니다.

### 서로 대응하는 것으로 확인한 3개 후보 구조

| `lua.package` zlib 시작 바이트 오프셋 | `lua64.package` 오프셋 | 함수 수 | 상수 수 | 스칼라 구조 SHA-256 |
|---:|---:|---:|---:|---|
| 974916 | 1482633 | 55 | 1181 | `a979c2832ad5765b90e8abd4a103e7285eb1d78646527abc04fb710d9c47a478` |
| 1017054 | 10430324 | 54 | 945 | `19ab19daa13c830166d93c49ab5f96438a7879fae3a903dd5c3566f4d30b875f` |
| 16702203 | 18061242 | 38 | 646 | `2fb80d3dc5bd9010601ff3f3e5ff097cc49cbc37f89e8c4e1213ce7ec8c80f30` |

각 행의 구조 해시는 함수별 명령 **개수**, 상수별 **타입·길이·앞 4바이트**, 자식 함수 **개수**로 계산했고 같은 행의 두 패키지에서 일치한다. 전체 바이너리가 동일하거나 같은 경로에 매핑된다는 뜻이 아니다. 후보의 실제 Lua 파일명, 일반 NPC 상점 폼과의 호출 관계, 판매 요청 패킷 번호·필드·정산 수식·클라이언트 활성 우선순위는 **아직 알 수 없다**.

## 테스트·변경 경계

- `python3 -m unittest -v test_audit_client_lua_sale_candidates.py`의 **합성 테스트 6개 PASS**: 32/64비트 스칼라, 명령어에 `sell`이 있어도 문자열 상수가 아니면 미검출, 깨진 LuaQ 구조, 해시 불일치, 잘린 zlib 및 잘못된 PCK 헤더 거부.
- 실제 두 패키지에서 위 SHA 검증·후보 수·세 쌍의 구조 SHA 일치 재현. 테스트는 원본 패키지와 PC DB·로그인 가능 서버 ZIP을 수정하지 않는다. 실제 데이터나 바이트코드를 공개 GitHub에 업로드하지 않는다.
- **일반 NPC 판매 기능 구현·수정: NOT DONE.** C2S 패킷, 서버 저장/정산, Bag/View, 실제 구매·판매/재접속 **LIVE/E2E: NOT RUN**. 이 조사 결과만으로 `SellPrice*`나 `OnSellClick`을 일반 판매 요청이라고 간주하지 않는다. GM 웹 지급과 mode3 Exchange 보류 유지.

## 다음 확인 지점

현재 BIN64의 실제 Lua 로더/PCK 파일 색인에서 **원본 파일 경로 → 후보 zlib 오프셋** 매핑과 일반 NPC 상점 폼의 버튼 이벤트 → 전송 함수 및 해당 필드가 확인되어야 패킷 의미를 확정할 수 있다. 해당 증거가 확보되지 않으면 추측하는 판매 핸들러를 추가하지 않고, 기존 구매·인벤토리 공통 경로의 별도 재현 가능한 결함을 먼저 조사한다.
